/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export'
};

module.exports = nextConfig;

// next.config.js
const nextConfig = {
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
